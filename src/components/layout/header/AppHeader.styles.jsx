import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  main: {
    flexGrow: 1,
    paddingBottom: '1vh',
  },
  title: {
    flexGrow: 1,
  },
}));
